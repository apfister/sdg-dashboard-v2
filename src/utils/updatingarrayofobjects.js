// } else {
//   mapRef.current.removeLayerFromMap(itemId);
// }

// if (isChecked) {
//   const item = {
//     itemId,
//   };

//   setAttributeLayers((prev) => [...prev, item]);
// } else {
//   let newItems = attributeLayers.filter((item) => item.itemId !== itemId);
//   setAttributeLayers(newItems);
// }
